# Zoo Management System - Backend

## Tech Stack
- Node.js
- Express
- Supabase (PostgreSQL)

## Setup Instructions

1. Clone the repo
```bash
git clone https://github.com/your-username/zoo-management-backend.git
cd zoo-management-backend
Install dependencies

bash Copy code
npm install
Setup environment variables

bash Copy code
cp .env.example .env
Fill Supabase credentials.

Run server

bash Copy code
npm start
Server runs on: http://localhost:3000

sql
Copy code

Commit README:

```bash
git add README.md
git commit -m "Add setup instructions"
git push







